= content_for :headings do
  .article-header-nav
    %ul.nav.nav-pills{:role => "tablist"}
      %li.nav-item
        %a.btn.btn-secondary{href: <%= singular_paths.join("_") %>_path} 戻る

.owner-container
  .container
    .card
      .card-header
        <%= model_name.name.classify %>の編集
      .card-body
        = render 'form'
