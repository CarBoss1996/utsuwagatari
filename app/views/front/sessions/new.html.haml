.container.front-new
  .card
    .card-body
      .form.mx-auto
        = simple_form_for :session, url: sessions_path, data: {turbo: false} do |f|
          = f.input :email
          = f.input :password
          .btn-section
            = f.button :submit, class: "btn btn-submit"
