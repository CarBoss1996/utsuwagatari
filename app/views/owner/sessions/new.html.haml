.container.owner-new
  .card
    .card-body
      .form.mx-auto
        = simple_form_for(:session, url: owner_sessions_path, html: {data: {turbo: false}}) do |f|
          = f.input :email
          = f.input :password
          = f.button :submit, class: "btn btn-submit"
